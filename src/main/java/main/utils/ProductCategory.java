package main.utils;

/**
 * An enumeration for Product categories, when a new product category
 * is added to the system, it just needs adding here.
 */
public enum ProductCategory {
    CLOTHES,
    FOOD,
    PHARMACY
}
